# 🛡️ Защита от ботов и накрутки

Проект использует многоуровневую систему защиты от злоупотреблений и ботов.

## 📊 Реализованная защита

### 1. Rate Limiting (Ограничение запросов)

**Backend функция:** `backend/rate_limiter/`

**Лимиты:**
- **60 запросов в минуту** с одного IP
- **1000 запросов в час** с одного IP  
- **10 запросов в минуту** на конкретный endpoint с одного IP
- **60 запросов в минуту** с одного устройства (fingerprint)

**Как работает:**
1. Каждый запрос логируется в таблицу `rate_limit_logs`
2. Проверяется история запросов за последнюю минуту/час
3. При превышении лимита - запрос блокируется
4. Автоматическая очистка логов старше 24 часов

### 2. Device Fingerprinting (Отпечаток устройства)

**Frontend:** `src/hooks/use-rate-limiter.ts`

Создается уникальный отпечаток браузера на основе:
- User Agent
- Язык браузера
- Разрешение экрана
- Глубина цвета
- Часовой пояс
- Canvas fingerprint
- Поддержка localStorage/sessionStorage

Это позволяет отслеживать пользователей даже при смене IP (VPN, мобильный интернет).

### 3. Client-Side Rate Limiting

Локальная проверка лимитов ДО отправки запроса на сервер:
- Хранит timestamp'ы последних запросов
- Блокирует запросы на клиенте при превышении лимита
- Уменьшает нагрузку на сервер

### 4. Page Visibility API

**Компоненты:** `Admin.tsx`, `SupportChat.tsx`

- Автоматически останавливает polling при неактивной вкладке
- Возобновляет при возврате пользователя
- Снижает нагрузку на 50-70% для фоновых вкладок

### 5. Smart Polling Optimization

- Проверка изменений перед обновлением UI
- Не перерисовывает компоненты без изменений
- Уменьшает потребление ресурсов браузера

## 🔧 Как использовать

### В React компонентах:

```typescript
import { useRateLimiter } from '@/hooks/use-rate-limiter';

const MyComponent = () => {
  const { checkRateLimit } = useRateLimiter({
    endpoint: 'my-feature',
    maxRequestsPerMinute: 10
  });

  const handleSubmit = async () => {
    // Проверка лимита
    const check = await checkRateLimit();
    if (!check.allowed) {
      toast({
        title: 'Ограничение запросов',
        description: check.reason,
        variant: 'destructive'
      });
      return;
    }

    // Выполнить запрос
    await fetch(...);
  };
};
```

### Временно отключить для компонента:

```typescript
const { checkRateLimit } = useRateLimiter({
  endpoint: 'my-feature',
  enabled: false  // Отключить проверку
});
```

## 📈 Мониторинг

### Получение статистики (только admin):

```bash
curl -H "X-Admin-Token: admin123" \
  "https://functions.poehali.dev/dd760420-6c65-41e9-bd95-171dec0f3ac9?action=get-stats"
```

**Ответ:**
```json
{
  "stats": {
    "endpoint_stats": [
      {
        "endpoint": "support-chat",
        "total_requests": 1523,
        "unique_ips": 45,
        "unique_devices": 52
      }
    ],
    "suspicious_ips": [
      {
        "ip_address": "1.2.3.4",
        "request_count": 850,
        "first_seen": "2025-12-11T10:00:00",
        "last_seen": "2025-12-11T11:30:00"
      }
    ]
  }
}
```

### Мониторинг в базе данных:

```sql
-- Топ IP по количеству запросов за последний час
SELECT 
  ip_address, 
  COUNT(*) as requests,
  COUNT(DISTINCT endpoint) as endpoints
FROM rate_limit_logs
WHERE created_at > NOW() - INTERVAL '1 hour'
GROUP BY ip_address
ORDER BY requests DESC
LIMIT 20;

-- Активность по endpoints
SELECT 
  endpoint,
  COUNT(*) as total_requests,
  COUNT(DISTINCT ip_address) as unique_ips
FROM rate_limit_logs
WHERE created_at > NOW() - INTERVAL '1 hour'
GROUP BY endpoint
ORDER BY total_requests DESC;
```

## 🚨 Что делать при атаке

### 1. Найти подозрительные IP:

```sql
SELECT ip_address, COUNT(*) as count
FROM rate_limit_logs
WHERE created_at > NOW() - INTERVAL '5 minutes'
GROUP BY ip_address
HAVING COUNT(*) > 100
ORDER BY count DESC;
```

### 2. Временно заблокировать IP на уровне функции:

Добавить в `backend/rate_limiter/index.py`:

```python
BLOCKED_IPS = ['1.2.3.4', '5.6.7.8']

def check_rate_limit(cursor, ip_address, endpoint, fingerprint):
    if ip_address in BLOCKED_IPS:
        return True, 'IP заблокирован администратором'
    # ... остальной код
```

### 3. Усилить лимиты:

Изменить константы в `check_rate_limit()`:
- Уменьшить `requests_per_minute` с 60 до 30
- Уменьшить `requests_per_hour` с 1000 до 500

## 📝 Защищенные компоненты

✅ **Уже защищены:**
- SupportChat (чат поддержки)
- Admin panel polling (автообновление админки)

⚠️ **Рекомендуется добавить:**
- Forum registration (регистрация на форуме)
- Forum post creation (создание постов)
- SMS verification (верификация телефонов)
- Appointment booking (запись на прием)

## 🎯 Best Practices

1. **Всегда используйте rate limiter для:**
   - Отправки форм
   - Создания записей (посты, комментарии, записи на прием)
   - SMS/Email верификации
   - Критичных операций (оплата, изменение данных)

2. **Настройте разные лимиты:**
   - Низкий (5-10 req/min) - критичные операции (SMS, регистрация)
   - Средний (10-20 req/min) - обычные формы
   - Высокий (30-60 req/min) - чтение данных

3. **Мониторинг:**
   - Раз в неделю проверяйте статистику
   - Ищите аномалии (один IP >> других)
   - Отслеживайте новые endpoints

4. **Оптимизация:**
   - Используйте Page Visibility API
   - Увеличивайте интервалы polling
   - Проверяйте изменения перед обновлением UI

## 🔐 Дополнительная защита (опционально)

### CAPTCHA для критичных операций:

```typescript
// Можно добавить hCaptcha или reCAPTCHA
import HCaptcha from '@hcaptcha/react-hcaptcha';

<HCaptcha
  sitekey="your-site-key"
  onVerify={(token) => handleVerify(token)}
/>
```

### IP Whitelist для админки:

```python
ADMIN_ALLOWED_IPS = ['192.168.1.1', '10.0.0.1']

if endpoint == 'admin' and ip_address not in ADMIN_ALLOWED_IPS:
    return True, 'Доступ запрещен с вашего IP'
```

## 📞 Поддержка

При вопросах по безопасности: https://t.me/+QgiLIa1gFRY4Y2Iy
